

const EmailsDataContainer = ({ children }) => {
    return (
        <div className="flex-grow flex overflow-x-hidden">
            {children}
        </div>
    )
}

export default EmailsDataContainer;
