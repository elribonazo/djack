

const EmailDetailsColumn = ({ children }) => {
    return (
        <div className="h-full w-full bg-white lg:block hidden">
            {children}
        </div>
    )
}

export default EmailDetailsColumn;
